const fontWeight = {
  semiBold: 600,
};

const colors = {
  red: 'red',
};

const typography = {
  h4: {
    fontSize: 20,
    fontWeight: fontWeight.semiBold,
    color: colors.red,
    lineHeight: 1.4,
  },
};

const theme = { typography };

export default theme;
